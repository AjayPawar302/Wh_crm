{
  "name": "wh-crm-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @whcrm/api & npm run dev -w @whcrm/web",
    "build": "npm run build -ws",
    "lint": "npm run lint -ws"
  }
}
